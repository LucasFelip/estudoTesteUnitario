package service;

import model.Usuario;
import model.Livro;

public class EmprestimoLivro {
    public Emprestimo alugarLivro(Usuario usuario, Livro livros){
        Arrays.stream(livros).forEach(livros -> {
            if (livros.get)
        });
    }
}